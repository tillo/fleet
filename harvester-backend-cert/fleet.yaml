# defaultNamespace (not namespace:) — the bundle includes cluster-scoped RBAC
# (ClusterRole/ClusterRoleBinding for the cluster-scoped ssl-certificates Setting),
# which a pinned namespace:/targetNamespace would forbid.
defaultNamespace: harvester-system
