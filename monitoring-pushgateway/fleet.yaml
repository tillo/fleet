defaultNamespace: monitoring
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: pushgateway
  repo: https://prometheus-community.github.io/helm-charts
  chart: prometheus-pushgateway
  version: 3.8.0  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
