defaultNamespace: split-horizon
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
dependsOn:
  # split-horizon owns the unbound deploy + the mdapi-registry pull secret this
  # bundle reuses; bring it up first.
  - name: mdapi-prod-split-horizon
  - name: mdapi-prod-external-secrets
helm:
  maxHistory: 25
  releaseName: external-dns-mdapi
