defaultNamespace: cert-manager
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: dev
helm:
  releaseName: cert-manager
  repo: https://charts.jetstack.io
  chart: cert-manager
  valuesFiles:
    - values.yml
