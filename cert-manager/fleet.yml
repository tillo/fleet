defaultNamespace: cert-manager
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: cert-manager
  repo: https://charts.jetstack.io
  chart: cert-manager
  version: v1.21.1  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
