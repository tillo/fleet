defaultNamespace: oauth2-proxy
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: main
helm:
  releaseName: oauth2-proxy-admin
  repo: https://oauth2-proxy.github.io/manifests
  chart: oauth2-proxy
  valuesFiles:
    - values.yml
