defaultNamespace: oauth2-proxy
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: oauth2-proxy-portal
  repo: https://oauth2-proxy.github.io/manifests
  chart: oauth2-proxy
  version: 10.7.0  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
