defaultNamespace: rook-ceph
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: rook-ceph
  repo: https://charts.rook.io/release
  chart: rook-ceph
  # Lane B (bootstrap-critical storage): PINNED + Renovate-tracked (fleet manager),
  # NOT Keel and NOT latest-tracking. Operator image docker.io/rook/ceph:v1.20.0
  # ships pullPolicy: IfNotPresent (verified in chart) so a cold cluster boots from
  # cache. Bump in lockstep with Harvester k8s upgrades (see ../../PLAN.md §1).
  version: v1.20.0
  valuesFiles:
    - values.yml
