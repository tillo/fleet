apiVersion: v1
kind: Service
metadata:
  name: chrony
  namespace: ntppool
spec:
  selector:
    app: chrony
  ports:
  - name: ntp
    port: 123
    protocol: UDP
    targetPort: 123
  type: LoadBalancer
