apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mirror-mdapi-ch
  namespace: ntppool
spec:
  secretName: mirror-mdapi-ch-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - mirror.mdapi.ch
