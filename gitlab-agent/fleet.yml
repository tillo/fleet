defaultNamespace: gitlab-agent
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: gitlab-agent-mdapi
  repo: https://charts.gitlab.io
  chart: gitlab-agent
  version: 2.30.0  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
