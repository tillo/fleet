defaultNamespace: gitlab-agent
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  historyMax: 25
  releaseName: gitlab-agent-mdapi
  repo: https://charts.gitlab.io
  chart: gitlab-agent
  valuesFiles:
    - values.yml
