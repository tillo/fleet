defaultNamespace: monitoring
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: monitoring-crds
  repo: https://prometheus-community.github.io/helm-charts
  chart: prometheus-operator-crds
  # Pinned. Bump in lockstep with the operator version in monitoring/fleet.yaml
  # (kube-prometheus-stack 84.5.0 ships operator v0.90.1; this CRD chart 28.0.1
  #  ships the matching v0.90.1 CRDs).
  version: "28.0.1"
