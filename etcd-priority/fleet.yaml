defaultNamespace: etcd-priority
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
  # Privileged hostPID node-tuner DaemonSet — relax PodSecurity for this ns.
  pod-security.kubernetes.io/enforce: privileged
helm:
  maxHistory: 25
