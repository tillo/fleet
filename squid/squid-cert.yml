apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: squid-cert
  namespace: squid
spec:
  dnsNames:
    - proxy.mdapi.ch
  issuerRef:
    kind: Issuer
    name: letsencrypt-prod
  secretName: squid-cert
