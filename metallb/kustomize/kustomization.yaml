# Post-renders the metallb chart (see ../fleet.yaml). Fleet feeds the
# helm-rendered manifests in as the kustomize base, then applies these patches
# (same pattern as cribl/ and envuassu/nextcloud/). Exists because the frr-k8s
# subchart values cannot reach the frr container's probe timeout — see the
# patch file for the full story.
patchesStrategicMerge:
- frr-liveness-timeout-patch.yaml
