# Post-renders the logstream-leader helm chart (see ../fleet.yml). No resources:
# here on purpose - Fleet feeds the helm-rendered manifests in as the kustomize
# base, then applies these patches (same pattern as envuassu/nextcloud/kustomize).
patchesStrategicMerge:
- logstream-leader-keel-patch.yaml
