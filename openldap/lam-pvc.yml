apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lam-data
spec:
  storageClassName: harvester-longhorn-1replica
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
