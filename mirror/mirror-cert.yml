apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mirror-cert
  namespace: mirror
spec:
  dnsNames:
    - 'mirror.mdapi.ch'
  issuerRef:
    kind: Issuer
    name: letsencrypt-prod
  secretName: mirror-cert
