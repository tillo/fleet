apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mirror-cert
  namespace: mirror
spec:
  dnsNames:
    - 'mirror.mdapi.ch'
    - 'tails.mdapi.ch'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: mirror-cert
