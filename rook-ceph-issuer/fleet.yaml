defaultNamespace: rook-ceph
helm:
  maxHistory: 25
# Raw manifests (cert-manager Issuer + TSIG ExternalSecret) for the ceph.mdapi.ch
# dashboard cert. Kept in a separate bundle because rook-ceph-cluster is a Helm
# bundle. Namespace rook-ceph is created/owned by the rook-ceph operator bundle.
