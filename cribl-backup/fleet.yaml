# Raw-manifest sibling of the helm-type cribl bundle: VolSync backup objects
# (G5, infra-jjb) — the helm bundle ignores stray raw files.
defaultNamespace: cribl
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
