apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: flame-env-secret
spec:
  refreshInterval: 1m

  secretStoreRef:
    kind: ClusterSecretStore
    name: cm-akeyless

  target:
    name: flame-env-secret
    creationPolicy: Owner

  data:
    - remoteRef:
        key: /mdapi/dev/flame/flame-env-secret/PASSWORD
      secretKey: PASSWORD
