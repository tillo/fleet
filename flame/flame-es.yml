apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: flame-env-secret
spec:
  refreshInterval: 1m0s

  secretStoreRef:
    kind: ClusterSecretStore
    name: cm-akeyless

  target:
    name: flame-env-secret
    creationPolicy: Owner

  data:
    - remoteRef:
        key: /mdapi/flame/flame-env-secret/PASSWORD
      secretKey: PASSWORD
