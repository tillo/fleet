apiVersion: v1
kind: Service
metadata:
  name: autoconf
  namespace: mail
spec:
  selector:
    app: autoconf
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
