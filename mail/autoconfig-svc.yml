apiVersion: v1
kind: Service
metadata:
  name: autoconfig
  namespace: mail
spec:
  selector:
    app: autoconfig
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
