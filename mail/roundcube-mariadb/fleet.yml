defaultNamespace: mail
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  releaseName: roundcube-mariadb
  chart: oci://registry-1.docker.io/cloudpirates/mariadb
  valuesFiles:
    - values.yml
