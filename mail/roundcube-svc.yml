apiVersion: v1
kind: Service
metadata:
  name: roundcube
  namespace: mail
spec:
  selector:
    app: roundcube
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
