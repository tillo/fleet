defaultNamespace: mail
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: docker-mailserver
  repo: https://docker-mailserver.github.io/docker-mailserver-helm
  chart: docker-mailserver
  version: 5.1.1  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
