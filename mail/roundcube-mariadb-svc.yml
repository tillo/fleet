apiVersion: v1
kind: Service
metadata:
  name: roundcube-mariadb
  namespace: mail
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: roundcube-mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
