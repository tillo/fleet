## Fleet wraps the HelmChartConfig as a plain manifest — RKE2's helm-controller
## reconciles it against the chart-managed CoreDNS release in kube-system.
## defaultNamespace covers the single manifest.
defaultNamespace: kube-system
