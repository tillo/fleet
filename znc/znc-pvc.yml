apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: znc-data
  namespace: znc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
