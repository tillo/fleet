## Public face for vmalert at vmalert.mdapi.ch. UI is read-mostly (rule
## groups, firing/pending alerts, notifier health). Same auth + WAF
## treatment as the rest of the stack — oauth2-proxy-admin gate +
## ModSecurity audit. cert-manager issues the TLS cert; external-dns
## creates the A record from this Ingress hostname.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vmalert
  namespace: monitoring
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    flame.pawelmalak/type: app
    flame.pawelmalak/name: vmalert
    flame.pawelmalak/url: https://vmalert.mdapi.ch
    flame.pawelmalak/category: Monitoring
    flame.pawelmalak/icon: bell-ring
    nginx.ingress.kubernetes.io/auth-response-headers: Authorization
    nginx.ingress.kubernetes.io/auth-signin: https://auth.mdapi.ch/admin/oauth2/start?rd=https://$host$escaped_request_uri
    nginx.ingress.kubernetes.io/auth-url: http://oauth2-proxy-admin.oauth2-proxy.svc.cluster.local/admin/oauth2/auth
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/modsecurity-transaction-id: "$request_id"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecAuditEngine RelevantOnly
      SecAuditLogRelevantStatus "^(?:5|4(?!04))"
      SecAuditLogParts ABIJDEFHZ
      SecAuditLogType Serial
      SecAuditLogFormat JSON
      SecAuditLog /dev/stdout
      SecDebugLog /tmp/modsec_debug.log
      Include /etc/nginx/owasp-modsecurity-crs/nginx-modsecurity.conf
spec:
  ingressClassName: nginx
  rules:
    - host: vmalert.mdapi.ch
      http:
        paths:
          - backend:
              service:
                name: vmalert-mdapi
                port:
                  number: 8080
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - vmalert.mdapi.ch
      secretName: vmalert-cert
