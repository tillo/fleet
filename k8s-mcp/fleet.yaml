defaultNamespace: k8s-mcp
namespace: k8s-mcp
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
targets:
  - clusterName: mdapi-prod
dependsOn:
  - name: mdapi-prod-cert-manager
  - name: mdapi-prod-external-secrets
helm:
  maxHistory: 25
