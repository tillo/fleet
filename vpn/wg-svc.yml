apiVersion: v1
kind: Service
metadata:
  name: wg-prod
  namespace: vpn
spec:
  type: NodePort
  selector:
    app: wg-prod
  ports:
    - protocol: UDP
      port: 51820
      targetPort: 51820
      nodePort: 31820
