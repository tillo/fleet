apiVersion: v1
kind: Service
metadata:
  name: transmission
  namespace: tv
spec:
  selector:
    app: transmission-pia
  ports:
    - name: http
      port: 9091
      protocol: TCP
      targetPort: 9091
  type: ClusterIP
