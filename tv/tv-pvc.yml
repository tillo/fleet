apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv
spec:
  storageClassName: harvester-longhorn-2replicas-notmigratable
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Ti
