apiVersion: v1
kind: Service
metadata:
  name: transmission
  namespace: tv
spec:
  selector:
    app: transmission-nordlynx
  ports:
    - name: http
      port: 9091
      protocol: TCP
      targetPort: 9091
  type: ClusterIP
