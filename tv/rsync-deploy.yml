apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsync
  namespace: tv
spec:
  selector:
    matchLabels:
      app: rsync
  replicas: 1
  template:
    metadata:
      labels:
        app: rsync
    spec:
      containers:
      - name: rsync
        image: stefda/rsync
        ports:
        - containerPort: 873
        env:
        - name: TZ
          value: Europe/Zurich
        - name: VOLUME
          value: /mnt/tv
        volumeMounts:
        - name: tv
          mountPath: /mnt/tv
      volumes:
      - name: tv
        persistentVolumeClaim:
          claimName: tv
