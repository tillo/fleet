apiVersion: v1
kind: Service
metadata:
  name: pms-docker
  namespace: tv
spec:
  selector:
    app: pms-docker
  ports:
    - name: http
      port: 32400
      protocol: TCP
      targetPort: 32400
  type: LoadBalancer
