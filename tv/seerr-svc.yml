apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: tv
spec:
  selector:
    app: seerr
  ports:
    - name: http
      port: 5055
      protocol: TCP
      targetPort: 5055
  type: ClusterIP
