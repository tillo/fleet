apiVersion: v1
kind: Service
metadata:
  name: autobrr
  namespace: tv
spec:
  selector:
    app: autobrr
  ports:
    - name: http
      port: 7474
      protocol: TCP
      targetPort: 7474
  type: ClusterIP
