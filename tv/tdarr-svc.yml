apiVersion: v1
kind: Service
metadata:
  name: tdarr
  namespace: tv
spec:
  selector:
    app: tdarr
  ports:
    - name: http
      port: 8265
      protocol: TCP
      targetPort: 8265
  type: ClusterIP
