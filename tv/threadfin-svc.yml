apiVersion: v1
kind: Service
metadata:
  name: threadfin
  namespace: tv
spec:
  selector:
    app: threadfin
  ports:
    - name: http
      port: 34400
      protocol: TCP
      targetPort: 34400
  type: ClusterIP
