apiVersion: v1
kind: Service
metadata:
  name: threadfin-bis
  namespace: tv
spec:
  selector:
    app: threadfin-bis
  ports:
    - name: http
      port: 34400
      protocol: TCP
      targetPort: 34400
  type: ClusterIP
