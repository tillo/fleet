# Dedicated bundle for the cluster-scoped longhorn-iomemory StorageClass.
# Uses defaultNamespace (NOT namespace:) so Fleet permits cluster-scoped objects.
# Kept separate from the namespace-pinned `iomemory` app bundle on purpose.
defaultNamespace: default
targets:
  - clusterName: mdapi-prod
