defaultNamespace: democratic-csi
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: democratic-csi-salt-iscsi
  repo: https://democratic-csi.github.io/charts/
  chart: democratic-csi
  version: 0.15.1  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
