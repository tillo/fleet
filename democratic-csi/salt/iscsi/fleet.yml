defaultNamespace: democratic-csi
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  historyMax: 25
  releaseName: democratic-csi-salt-iscsi
  repo: https://democratic-csi.github.io/charts/
  chart: democratic-csi
  valuesFiles:
    - values.yml
