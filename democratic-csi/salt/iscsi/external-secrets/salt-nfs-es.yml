apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: salt-iscsi-driver-config
spec:
  refreshInterval: 1m0s

  secretStoreRef:
    kind: ClusterSecretStore
    name: cm-akeyless

  target:
    name: salt-iscsi-driver-config
    creationPolicy: Owner
    template:
      data:
        driver-config-file.yaml: '{{ .saltIscsiDriverConfig | toString }}'

  data:
    - remoteRef:
        key: /mdapi/democratic-csi/salt-iscsi-driver-config
      secretKey: saltIscsiDriverConfig
