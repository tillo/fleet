defaultNamespace: democratic-csi
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  releaseName: democratic-csi-salt-nfs
  repo: https://democratic-csi.github.io/
  chart: democratic-csi
  valuesFiles:
    - values.yml
