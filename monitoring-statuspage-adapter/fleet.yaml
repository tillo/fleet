defaultNamespace: monitoring
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
dependsOn:
  # ExternalSecrets must be installed before our ExternalSecret will reconcile
  - name: mdapi-prod-external-secrets
helm:
  maxHistory: 25
