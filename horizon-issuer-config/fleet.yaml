defaultNamespace: horizon-issuer
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
# The ClusterIssuer needs the CRDs from the controller bundle first.
dependsOn:
  - name: mdapi-prod-horizon-issuer
