apiVersion: v1
kind: Endpoints
metadata:
  name: harvester
  namespace: transit
subsets:
  - addresses:
      - ip: 192.168.1.191   # cluster VIP — Harvester is the default backend
    ports:
      - port: 443
