apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: transit
spec:
  type: ClusterIP
  ports:
    - name: api
      port: 30000
      targetPort: 30000
    - name: console
      port: 30001
      targetPort: 30001
