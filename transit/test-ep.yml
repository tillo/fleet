apiVersion: v1
kind: Endpoints
metadata:
  name: test
  namespace: transit
subsets:
  - addresses:
      - ip: 10.53.99.99   # pinned vpn/wg-prod-proxy ClusterIP — chains to socat sidecar in wg-prod that forwards via wg0 to 10.200.0.2:443
    ports:
      - port: 443
