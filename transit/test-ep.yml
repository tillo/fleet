apiVersion: v1
kind: Endpoints
metadata:
  name: test
  namespace: transit
subsets:
  - addresses:
      - ip: 10.200.0.2   # WireGuard VPN IP for test cluster gateway pod
    ports:
      - port: 443
