apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pfsense
  namespace: transit
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
spec:
  ingressClassName: nginx
  rules:
    - host: pfsense.mdapi.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pfsense
                port:
                  number: 443
  tls:
    - hosts:
        - pfsense.mdapi.ch
      secretName: pfsense-cert
