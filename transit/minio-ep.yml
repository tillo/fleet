apiVersion: v1
kind: Endpoints
metadata:
  name: minio
  namespace: transit
subsets:
  - addresses:
      - ip: 192.168.1.191   # TODO: verify — api.minio.home.tillo.ch resolves here; confirm console is same host
    ports:
      - name: api
        port: 30000
      - name: console
        port: 30001
