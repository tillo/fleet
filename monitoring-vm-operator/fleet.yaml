defaultNamespace: monitoring
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  takeOwnership: true
  releaseName: vm-operator
  repo: https://victoriametrics.github.io/helm-charts/
  chart: victoria-metrics-operator
  version: 0.65.1  # Renovate-managed (renovate.json)
  valuesFiles:
    - values.yml
