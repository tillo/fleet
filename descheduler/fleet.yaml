defaultNamespace: descheduler
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: descheduler
  repo: https://kubernetes-sigs.github.io/descheduler/
  chart: descheduler
  # Pinned to the k8s-1.35 matching line. Bump deliberately alongside RKE2.
  version: 0.36.0
  valuesFiles:
    - values.yml
