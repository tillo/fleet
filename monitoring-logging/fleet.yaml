defaultNamespace: cattle-logging-system
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: rancher-logging
  repo: https://charts.rancher.io
  chart: rancher-logging
  ## Match the rancher-logging-crd already installed (107.0.1+up4.10.0-rancher.10).
  ## App version 4.10.0; rancher iteration .13 is the closest available app chart.
  version: "107.0.1+up4.10.0-rancher.13"
  valuesFiles:
    - values.yml
