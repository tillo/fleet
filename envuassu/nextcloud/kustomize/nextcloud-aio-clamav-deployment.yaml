apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud-aio-clamav
  namespace: envuassu
spec:
  template:
    spec:
      containers:
        - name: nextcloud-aio-clamav
          imagePullPolicy: IfNotPresent
