apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncloud-data-ceph
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 700Gi
