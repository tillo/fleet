## Fleet wraps the HelmChartConfig as a plain manifest — RKE2's helm-controller
## reconciles it against the chart-managed ingress-nginx release in kube-system.
## Same shape as rke2-coredns-config/, which is the precedent for owning an RKE2
## packaged-component override from git rather than from a node-local manifest.
defaultNamespace: kube-system
