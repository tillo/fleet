apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome-config
spec:
  storageClassName: harvester-longhorn-2replicas
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
