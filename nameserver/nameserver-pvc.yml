apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nameserver-zones
  namespace: nameserver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
