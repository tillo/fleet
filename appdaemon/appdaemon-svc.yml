apiVersion: v1
kind: Service
metadata:
  name: appdaemon
  namespace: appdaemon
spec:
  selector:
    app: appdaemon
  ports:
    - name: appdaemon
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
