defaultNamespace: keel
namespaceLabels:
  cd.mdapi.ch: fleet
  cdenv.mdapi.ch: prod
helm:
  maxHistory: 25
  releaseName: keel
  repo: https://keel-hq.github.io/keel/
  chart: keel
  # Pinned + bumped by Renovate (fleet manager, see renovate.json). Keel can't
  # cleanly self-update its own chart, so Renovate owns the version. Was: unpinned.
  version: v1.0.5
  valuesFiles:
    - values.yml
