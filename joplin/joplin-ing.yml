apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: joplin
  namespace: joplin
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    flame.pawelmalak/type: app
    flame.pawelmalak/name: Joplin
    flame.pawelmalak/url: https://notes.mdapi.ch
    flame.pawelmalak/category: Collaboration
    flame.pawelmalak/icon: message-text
    flame.pawelmalak/order: '50'
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/modsecurity-transaction-id: "$request_id"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecAction "id:900200,phase:1,pass,nolog,setvar:tx.allowed_methods=GET HEAD POST OPTIONS PUT PATCH DELETE"
      Include /etc/nginx/owasp-modsecurity-crs/nginx-modsecurity.conf
      SecRuleRemoveById 941100 942100 932100 932105 932106 932110 932115 932120 932130 932140 932150 932160 932170 932180
spec:
  ingressClassName: nginx
  rules:
    - host: notes.mdapi.ch
      http:
        paths:
          - backend:
              service:
                name: joplin
                port:
                  number: 22300
            path: /
            pathType: Prefix
  tls:
    - hosts:
      - notes.mdapi.ch
      secretName: joplin-cert
